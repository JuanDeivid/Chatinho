<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chat Bot</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #4f46e5, #6d28d9);
            font-family: Arial, Helvetica, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            color: #fff;
        }

        .chatbox {
            background: rgba(255, 255, 255, 0.15);
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3);
            backdrop-filter: blur(12px);
            text-align: center;
            width: 300px;
        }

        .chatbox h2 {
            margin-bottom: 20px;
            font-size: 26px;
            font-weight: bold;
        }

        .chatbox button {
            padding: 12px 25px;
            background: #10b981;
            border: none;
            border-radius: 10px;
            color: white;
            font-size: 16px;
            cursor: pointer;
            transition: 0.3s;
        }

        .chatbox button:hover {
            background: #00ffb3;
            transform: scale(1.05);
        }
    </style>
</head>

<body>
    <div class="chatbox">
        <h2>ChatBot por voz</h2>
        <button onclick="ouvir()">Falar Agora</button>
        <p id="transcricao"></p>
    </div>

    <script>
        const pergunta = [
            //qual o hormonio responsavel pela produção do leite materno
            "leite materno",
            //como deve ser feito o cuidado com o coto umbilical
            "coto umbilical",
            //o que deve ser feito antes de manipular o recém-nascido
            "manipular o recém-nascido",
            //qual nome do leite produzido nos primeiros dias após parto,rico em imunoglobulinas
            "leite produzido"
        ];

        const resposta = [
            "A produção do leite é feita pelo hormônio prolactina. Já o hormônio responsável pela descida ou ejeção do leite é a ocitocina.",
            "Manter sempre limpo e seco. Higienização com álcool 70%, se essa for a recomendação local.",
            "Lavar bem as mãos com água e sabão ou usar álcool 70%, além de usar EPIs.",
            "O colostro é o primeiro leite materno, rico em imunoglobulinas."
        ];
        
        function falar(texto) {
            const discurso = new SpeechSynthesisUtterance(texto);
            discurso.lang ="pt-BR";
            discurso.rate = 1;

            document.getElementById("transcricao").innerHTML +=
                "<strong>Chatbot:</strong> " + texto + "<br>";

            window.speechSynthesis.speak(discurso);
        }

        function ouvir() {
            const reconhecer = new webkitSpeechRecognition();
            reconhecer.lang = "pt-BR";
            reconhecer.start();

            reconhecer.onresult = function (event) {
                const textoFalado = event.results[0][0].transcript.toLowerCase();

                document.getElementById("transcricao").innerHTML +=
                    "<strong>Você:</strong> " + textoFalado + "<br>";

                for (let i = 0; i < pergunta.length; i++) {
                    if (textoFalado.includes(pergunta[i])) {
                        falar(resposta[i]);
                        return;
                    }
                }

                falar("Desculpe, eu não entendi.");
            };
        }
    </script>
</body>
</html>
